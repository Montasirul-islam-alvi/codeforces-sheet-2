# codeforces-sheet-2
#include<iostream>
using namespace std;
int main()
{
    int k,s,x,y,z,cnt=0;
    cin>>k>>s;
    for(x = 0; x <= k;x++)
    {
     for(y = 0; y <= k;y++)
     {
        z = (s-(x+y));
        if(0 <= z && z <= k)cnt++;
     }
    }
    cout<<cnt<<endl;
}
